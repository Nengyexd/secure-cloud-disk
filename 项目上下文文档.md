# 📘 基于AES加密的安全云盘系统 - 项目上下文文档

## 一、项目概要（Project Overview）

**项目名称**：基于AES加密的安全云盘系统（吉亦云盘）

**项目背景**：
- 这是一个毕业设计项目
- 采用端到端加密架构，用户主密钥仅存储在客户端，服务器无法解密用户文件
- 实现文件的加密存储、分享、管理等完整云盘功能

**目标与目的**：
- 提供一个安全可靠的云存储服务
- 实现文件的端到端加密保护
- 支持VIP会员体系和存储配额管理
- 提供完整的文件管理功能（上传、下载、预览、分享、收藏等）

**要解决的问题**：
- 云存储的数据安全性（端到端加密）
- 文件秒传优化（基于SHA-256哈希去重）
- 大文件上传下载的性能优化
- 跨浏览器下载管理器拦截问题（IDM等工具的干扰）
- 拖拽操作的用户体验优化

**整体愿景**：
构建一个功能完善、安全可靠、用户体验良好的个人云盘系统

---

## 二、范围定义（Scope Definition）

**当前范围**：
1. 用户认证与授权（邮箱验证码登录/注册）
2. 文件管理（上传、下载、预览、删除、重命名、移动）
3. 文件夹管理（创建、嵌套）
4. 文件分享（带密码、过期时间）
5. 文件收藏功能
6. 回收站机制
7. VIP会员系统（订单、提醒、权益）
8. 操作日志记录
9. 存储配额管理
10. 图片缩略图生成
11. 拖拽上传与内部拖拽移动
12. 批量下载（ZIP打包）
13. 右键快捷菜单
14. 文件分类浏览（图片、视频、音频、文档）
15. 最近上传记录
16. 上传/下载速度显示
17. 数据统计可视化
18. 文件排序和筛选

**非本次范围**：
- 移动端应用
- 文件协作编辑
- 文件版本控制
- 在线预览Office文档
- 第三方支付集成（当前仅模拟支付）

**约束条件**：
- 普通用户：单文件最大500MB，总存储5GB
- VIP用户：单文件最大2GB，总存储100GB
- 加密方式：AES-256-GCM（不可变更）
- 主密钥仅存储在客户端（安全架构约束）
- 浏览器必须支持Web Crypto API

---

## 三、关键实体与关系（Key Entities & Relationships）

**核心实体**：
- User（用户）
- File（文件）
- Share（分享）
- Favorite（收藏）
- VipOrder（VIP订单）
- VipReminder（VIP提醒）
- OperationLog（操作日志）
- LoginLog（登录日志）
- EmailVerificationCode（邮箱验证码）

**实体职责**：
- **User**：管理用户信息、存储配额、VIP状态、主密钥（加密后）
- **File**：管理文件元数据、加密密钥、存储位置、文件夹层级关系
- **Share**：管理分享链接、访问密码、过期时间、访问统计
- **Favorite**：记录用户收藏的文件
- **VipOrder**：记录VIP购买订单、支付状态
- **VipReminder**：VIP到期提醒记录
- **OperationLog**：记录所有用户操作行为
- **LoginLog**：记录用户登录历史
- **EmailVerificationCode**：邮箱验证码管理

**实体关系描述**：
- User 1:N File（一个用户拥有多个文件）
- File N:1 File（文件夹的父子关系，通过parent_id实现）
- User 1:N Share（一个用户可创建多个分享）
- File 1:N Share（一个文件可被多次分享）
- User 1:N Favorite（一个用户可收藏多个文件）
- User 1:N VipOrder（一个用户可有多个订单）
- User 1:N OperationLog（一个用户有多条操作日志）

---

## 四、功能模块拆解（Functional Decomposition）

**模块列表**：
1. 认证模块（Authentication）
2. 文件管理模块（File Management）
3. 加密/解密模块（Encryption）
4. 存储模块（Storage - MinIO）
5. 分享模块（Share）
6. VIP模块（VIP System）
7. 统计模块（Statistics）
8. 日志模块（Logging）

**模块详情**：

### 认证模块
- **输入**：邮箱、验证码或密码
- **输出**：JWT Token、用户信息、主密钥（加密后）
- **核心逻辑**：
  - 邮箱验证码注册/登录
  - 密码使用PBKDF2加密存储
  - 主密钥使用用户密码派生密钥加密后存储在服务器
  - 登录成功后返回加密的主密钥给客户端解密

### 文件管理模块
- **输入**：文件流、文件元数据、用户主密钥
- **输出**：加密文件、文件记录、操作结果
- **核心逻辑**：
  - **上传流程**：
    1. 计算文件SHA-256哈希
    2. 检查是否可秒传（相同哈希）
    3. 生成随机AES-256密钥
    4. 使用文件密钥加密文件（AES-GCM）
    5. 使用主密钥加密文件密钥
    6. 上传加密文件到MinIO
    7. 保存文件元数据到数据库
  - **下载流程**：
    1. 从MinIO获取加密文件
    2. 使用主密钥解密文件密钥
    3. 使用文件密钥解密文件
    4. 返回原始文件给用户
  - **拖拽移动**：在前端验证目标文件夹，调用移动API
  - **批量下载**：前端使用JSZip打包多个解密后的文件

### 加密/解密模块
- **输入**：明文数据、密钥
- **输出**：加密数据（IV + 密文 + Tag）
- **核心逻辑**：
  - 使用Web Crypto API（浏览器端）
  - 使用Java Cipher（服务器端）
  - AES-256-GCM模式，128位Tag
  - 12字节随机IV

### 存储模块（MinIO）
- **输入**：加密文件字节流、存储键
- **输出**：存储成功/失败状态
- **核心逻辑**：
  - 文件以UUID作为存储键
  - 缩略图以"thumb_"+UUID存储
  - 支持流式上传/下载

### 分享模块
- **输入**：文件ID、访问密码、过期时间
- **输出**：分享码、分享链接
- **核心逻辑**：
  - 生成10位随机分享码
  - 支持密码保护（可选）
  - 支持过期时间设置（VIP可永久）
  - 记录访问和下载次数

### VIP模块
- **输入**：用户ID、套餐类型、支付信息
- **输出**：订单记录、VIP到期时间
- **核心逻辑**：
  - 三种套餐：月卡（30天）、季卡（90天）、年卡（365天）
  - 到期提醒：7天、3天、1天、已过期
  - 模拟支付流程

**典型用户场景**：
1. 用户注册：输入邮箱 → 接收验证码 → 设置密码 → 生成主密钥 → 完成注册
2. 文件上传：选择文件 → 前端加密 → 上传到服务器 → MinIO存储 → 更新配额
3. 文件分享：选择文件 → 设置密码和有效期 → 生成分享链接 → 分享给他人
4. VIP购买：选择套餐 → 模拟支付 → 更新用户类型和到期时间 → 享受更大配额

---

## 五、技术方向与关键决策（Technical Direction & Decisions）

**客户端**：
- Vue 3 + Composition API
- Element Plus UI组件库
- Axios（HTTP请求）
- JSZip（批量下载打包）
- Thumbnailator（图片缩略图）
- ECharts（数据可视化）
- Web Crypto API（浏览器原生加密）

**服务端**：
- Spring Boot 2.7.x
- MyBatis Plus（ORM）
- Spring Security + JWT（认证授权）
- MinIO（对象存储）
- MySQL 8.0（数据库）
- Lombok（简化代码）

**模型或算法层**：
- **加密算法**：AES-256-GCM
- **密钥派生**：PBKDF2（用户密码）
- **文件哈希**：SHA-256（秒传去重）
- **密钥管理**：三层密钥架构
  - 用户密码 → 派生密钥（PBKDF2）
  - 派生密钥 → 加密主密钥（存储在服务器）
  - 主密钥 → 加密文件密钥
  - 文件密钥 → 加密文件内容

**数据流与架构**：
```
[客户端] ↔ [JWT认证] ↔ [Spring Boot后端] ↔ [MySQL]
                                        ↔ [MinIO对象存储]

加密流：
用户文件 → AES-GCM加密 → MinIO存储
    ↓
文件密钥 → 主密钥加密 → 数据库存储
```

**已做技术决策**：
1. ✅ **端到端加密**：主密钥仅存在客户端，服务器无法解密用户文件
2. ✅ **ByteArrayResource代替InputStreamResource**：解决IDM等下载管理器拦截导致的HTTP 204问题
3. ✅ **前端JSZip批量下载**：保持端到端加密，避免服务器端解密
4. ✅ **缩略图服务端生成**：上传时生成并加密，优化用户体验
5. ✅ **拖拽来源识别**：通过dataTransfer.types区分外部文件和内部拖拽
6. ✅ **SHA-256秒传**：基于内容哈希实现文件去重和秒传
7. ✅ **统一数据库目录**：所有SQL脚本集中到database/目录管理

**可替代方案**：
- 对象存储：MinIO → 阿里云OSS / 腾讯云COS（如需生产部署）
- 数据库：MySQL → PostgreSQL（如需更强的JSON支持）
- 前端框架：Vue 3 → React / Angular（技术栈偏好）
- 加密库：Web Crypto API → CryptoJS（兼容性考虑）

---

## 六、交互、风格与输出约定（Interaction & Style Conventions）

**AI 输出风格**：
- 结构清晰、层级明确、工程化表达
- 中文沟通，代码注释保持简洁
- 优先使用专业术语，必要时解释

**表达规范**：
- 统一使用Markdown格式
- 代码块使用语言标识（java, javascript, sql等）
- 重要步骤使用有序列表
- 技术决策使用表格或对比说明

**格式要求**：
- 代码必须可直接运行，无占位符
- SQL脚本保持CREATE TABLE IF NOT EXISTS安全模式
- 文件路径使用绝对路径（Z:\大学Study\AAA毕业设计\基于AES加密的安全云盘系统\...）
- 日志输出保持简洁，生产环境移除调试日志

**用户特殊偏好**：
- 分步完成复杂任务，每步确认后再继续
- 遇到问题时提供详细的错误日志和解决方案
- 优先保证代码简洁性，避免过度工程化
- 注释简化，代码自说明优先

---

## 七、当前进展总结（Current Status）

**已确认事实**：

✅ **功能开发完成**：
- 用户认证系统（邮箱验证码、密码登录）
- 文件CRUD操作（上传、下载、预览、删除、重命名、移动）
- 文件夹嵌套管理
- 文件分享（带密码、过期时间）
- 文件收藏功能
- 回收站（软删除、恢复、永久删除）
- VIP会员系统（订单、提醒、权益）
- 操作日志完整记录
- 图片缩略图生成
- 拖拽上传功能
- 拖拽移动文件到文件夹
- 批量下载（ZIP打包）
- 右键快捷菜单
- 文件分类视图（图片、视频、音频、文档）
- 最近上传记录
- 上传/下载速度显示
- 数据统计图表（ECharts）
- 文件排序和筛选

✅ **Bug修复完成**：
1. MP3等文件下载失败（IDM拦截）→ 改用ByteArrayResource + 禁用IDM
2. 缩略图加载导致服务器过载 → 批量加载+并发限制（max 3）
3. 拖拽时误显示上传提示框 → 通过dataTransfer.types识别拖拽来源
4. 数据库字段缺失 → 统一SQL脚本管理

✅ **代码优化完成**：
1. 清理前端调试代码（Home.vue, aes.js）
2. 清理后端调试日志（FileService.java, FileController.java）
3. 删除重复注释代码（约150行）
4. 整理数据库SQL文件到统一目录（database/）
5. 删除分散的SQL文件（backend/sql/, backend/src/main/resources/sql/）
6. 创建数据库使用文档（database/README.md）

**未解决问题**：
- 无（当前所有已知问题已解决）

---

## 八、后续计划与风险（Next Steps & Risks）

**待讨论主题**：
- 是否需要实现移动端（H5或原生App）
- 是否需要集成真实支付接口（支付宝/微信）
- 是否需要增加文件版本控制功能
- 是否需要增加文件协作功能
- 部署方案（本地/云服务器）

**潜在风险与不确定性**：
1. **性能风险**：
   - 大量小文件上传可能导致MinIO性能瓶颈
   - 并发下载可能占用大量服务器内存（ByteArrayResource全加载）
   - 建议：生产环境考虑CDN加速、流式下载优化

2. **安全风险**：
   - 主密钥丢失无法恢复文件（用户需妥善保管）
   - 分享链接可能被爬虫扫描
   - 建议：增加验证码机制、限制分享访问频率

3. **兼容性风险**：
   - Web Crypto API在旧版浏览器不支持
   - 建议：增加浏览器兼容性检测和提示

4. **存储成本**：
   - MinIO本地部署需要足够磁盘空间
   - 建议：实施存储配额监控和清理策略

**推荐的后续初始化 Prompt**：

```markdown
# 项目上下文注入

你正在协助开发"基于AES加密的安全云盘系统"项目，这是一个毕业设计项目。

## 核心架构
- 前端：Vue 3 + Element Plus
- 后端：Spring Boot + MyBatis Plus + MinIO
- 加密：AES-256-GCM端到端加密，主密钥仅存在客户端

## 当前状态
所有核心功能已开发完成，包括：文件管理、分享、VIP系统、数据统计等。
最近完成的优化：
1. 修复了IDM拦截导致的下载失败问题
2. 优化了拖拽功能，区分外部上传和内部移动
3. 完成了代码清理和SQL脚本整理

## 开发规范
- 代码简洁优先，避免过度注释
- 分步实施，每步确认
- 生产代码不包含调试日志
- 所有数据库脚本集中在 database/ 目录

请基于以上上下文继续协助项目开发和优化。
```

---

## 补充：术语词典（Glossary）

| 术语 | 含义 |
|------|------|
| **主密钥（Master Key）** | 用户的AES-256密钥，用于加密所有文件密钥，仅存储在客户端 |
| **文件密钥（File Key）** | 每个文件独立的AES-256密钥，用于加密文件内容 |
| **秒传** | 基于SHA-256哈希检测相同文件，直接引用已有文件，无需重复上传 |
| **端到端加密（E2EE）** | 文件在客户端加密，服务器仅存储密文，无法解密 |
| **AES-GCM** | 一种认证加密模式，同时提供保密性和完整性 |
| **MinIO** | 兼容S3协议的开源对象存储服务 |
| **PBKDF2** | 基于密码的密钥派生函数，用于从用户密码生成加密密钥 |
| **JWT** | JSON Web Token，用于无状态的用户认证 |
| **ByteArrayResource** | Spring框架的资源类型，将完整字节数组作为响应体 |
| **IDM** | Internet Download Manager，浏览器下载管理器扩展 |

---

## 项目目录结构

```
基于AES加密的安全云盘系统/
├── backend/                  # 后端Spring Boot项目
│   ├── src/
│   │   ├── main/
│   │   │   ├── java/com/clouddisk/
│   │   │   │   ├── config/          # 配置类
│   │   │   │   ├── controller/      # 控制器
│   │   │   │   ├── service/         # 业务逻辑
│   │   │   │   ├── mapper/          # MyBatis映射
│   │   │   │   ├── entity/          # 实体类
│   │   │   │   ├── dto/             # 数据传输对象
│   │   │   │   ├── vo/              # 视图对象
│   │   │   │   └── util/            # 工具类
│   │   │   └── resources/
│   │   │       └── application.yml  # 应用配置
│   │   └── test/                    # 测试代码
│   └── pom.xml                      # Maven依赖
│
├── frontend/                 # 前端Vue 3项目
│   ├── src/
│   │   ├── api/             # API请求封装
│   │   ├── components/      # 公共组件
│   │   ├── views/           # 页面视图
│   │   ├── store/           # Pinia状态管理
│   │   ├── router/          # 路由配置
│   │   ├── utils/           # 工具函数
│   │   │   ├── aes.js       # AES加密工具
│   │   │   └── request.js   # Axios封装
│   │   └── App.vue          # 根组件
│   ├── package.json         # NPM依赖
│   └── vite.config.js       # Vite配置
│
├── database/                 # 数据库脚本（统一管理）
│   ├── init.sql             # 完整初始化脚本
│   ├── update.sql           # 数据库迁移脚本
│   └── README.md            # 数据库使用说明
│
└── PROJECT_CONTEXT.md        # 本文档
```

---

**文档版本**：v1.0
**最后更新**：2026-01-10
**项目路径**：`Z:\大学Study\AAA毕业设计\基于AES加密的安全云盘系统`

---

## 快速开始指南

### 1. 环境准备
- JDK 11+
- Node.js 16+
- MySQL 8.0
- MinIO（Docker或本地安装）

### 2. 数据库初始化
```bash
mysql -u root -p < database/init.sql
```

### 3. 后端启动
```bash
cd backend
mvn spring-boot:run
```

### 4. 前端启动
```bash
cd frontend
npm install
npm run dev
```

### 5. MinIO配置
```yaml
# backend/src/main/resources/application.yml
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: cloud-disk
```

### 6. 访问系统
- 前端地址：http://localhost:5173
- 后端API：http://localhost:8080
- MinIO控制台：http://localhost:9001

---

## 常见问题（FAQ）

**Q: 主密钥丢失怎么办？**
A: 主密钥丢失后无法恢复文件，这是端到端加密的特性。建议用户在安全的地方备份主密钥。

**Q: 如何提高大文件上传速度？**
A: 可以考虑实现分片上传、断点续传等功能。

**Q: MinIO存储空间不足？**
A: 可以扩展MinIO存储、实施文件清理策略，或迁移到云对象存储服务。

**Q: 如何部署到生产环境？**
A: 需要配置HTTPS、修改CORS策略、优化数据库连接池、配置反向代理等。

---

## 联系方式

- 项目负责人：暂无信息
- 邮箱：暂无信息
- GitHub：暂无信息

---

**许可证**：暂无信息
**致谢**：感谢所有开源项目的贡献者
