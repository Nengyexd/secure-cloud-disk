我将修复 `Home.vue` 中 `handleRowContextMenu` 函数重复声明导致的编译错误。

### 问题原因
代码中存在两个 `handleRowContextMenu` 函数声明：
1.  第 1355 行：第一次声明。
2.  第 2212 行：重复声明。

这导致了 Vue 编译时报错 "Identifier 'handleRowContextMenu' has already been declared"。

### 修复计划
1.  **删除冗余声明**：我将删除第 2212 行处的重复函数声明及其相关辅助逻辑，因为第 1355 行处的实现已经包含了所需的逻辑。
2.  **保留必要逻辑**：确保删除后的代码逻辑完整，特别是右键菜单的关闭逻辑（点击其他区域关闭菜单）。第 1355 行的实现已经包含了基础逻辑，但我会检查是否需要合并第 2212 行附近的增强逻辑（如添加全局点击监听器）。

### 执行步骤
1.  使用 `SearchReplace` 工具删除第 2212 行开始的重复 `handleRowContextMenu` 函数块。
2.  确认文件不再有其他重复声明。
